part of babylon;

/// class SubSurfaceScatteringPostProcess
@JS()
class SubSurfaceScatteringPostProcess extends PostProcess {
  external SubSurfaceScatteringPostProcess(String name, Scene scene, dynamic options, [Camera camera, num samplingMode, Engine engine, bool reusable, num textureType]);
  external String getClassName();
}
