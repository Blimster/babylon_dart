part of babylon;

/// class PassPostProcess
@JS()
class PassPostProcess extends PostProcess {
  external PassPostProcess(String name, dynamic options, [Camera camera, num samplingMode, Engine engine, bool reusable, num textureType, bool blockCompilation]);
  external String getClassName();
}
