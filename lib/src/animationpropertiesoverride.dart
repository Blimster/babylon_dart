part of babylon;

/// class AnimationPropertiesOverride
@JS()
class AnimationPropertiesOverride {
  external bool get enableBlending;
  external num get blendingSpeed;
  external num get loopMode;
  external set enableBlending(bool enableBlending);
  external set blendingSpeed(num blendingSpeed);
  external set loopMode(num loopMode);
}
